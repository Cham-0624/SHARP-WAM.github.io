if(window.parent!==window){let previousHeight=0;const reportHeight=()=>{const height=Math.ceil(document.querySelector('main').getBoundingClientRect().height);if(height!==previousHeight){previousHeight=height;window.parent.postMessage({type:'sharp-method-height',height},location.origin);}};new ResizeObserver(reportHeight).observe(document.querySelector('main'));window.addEventListener('load',reportHeight);reportHeight();}
